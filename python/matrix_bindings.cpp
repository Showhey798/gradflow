#include <pybind11/pybind11.h>
#include <pybind11/stl.h>

// #include <gradflow/matrix.hpp>

namespace py = pybind11;

void bind_matrix(py::module_& m) {
    // TODO: Implement matrix bindings
    // Example:
    // py::class_<fullscratch::Matrix<double>>(m, "Matrix")
    //     .def(py::init<size_t, size_t>())
    //     .def("rows", &fullscratch::Matrix<double>::rows)
    //     .def("cols", &fullscratch::Matrix<double>::cols);
}
