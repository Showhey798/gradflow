@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Eigen3 3.4)

# Include targets
if(NOT TARGET FullScratchML::fullscratch)
    include("${CMAKE_CURRENT_LIST_DIR}/FullScratchMLTargets.cmake")
endif()

check_required_components(FullScratchML)
